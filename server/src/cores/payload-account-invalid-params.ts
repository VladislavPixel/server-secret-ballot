import type { IPayloadAccountInvalidParams } from '../types';

const payloadAccountInvalidParams: IPayloadAccountInvalidParams = {
	error: {
		message: 'INVALID_PARAMS_ACCOUNT',
		code: 400
	}
};

module.exports = payloadAccountInvalidParams;
