import type { IPayloadVoiceInvalidParams } from '../types';

const payloadVoiceInvalidParams: IPayloadVoiceInvalidParams = {
	error: {
		message: 'INVALID_PARAMS_VOICE',
		code: 400
	}
};

module.exports = payloadVoiceInvalidParams;
